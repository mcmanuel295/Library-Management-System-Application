services:
  book-db:
    container_name: book-db
    image: mysql
    networks:
      - library_network
      -
    volumes:
      - var:lib/var/app/db

    environment:
      - MYDQL_password: Oea75357@
      - MYSQL_USER: root

    ports:
      - 3306:3306



  book-service:
    container_name: mcmanuel295/bsi
    networks:
      -library_network


volumes:
  library_network:
    driver: local

networks:
