name: BookService pipeLine

on:
  push:
    branches:
      - main

    paths:
      - .github/workflows/Book-Pipeline.yml
      - Books/src/**
      - Docker/Book/**
      - "docker-compose.yml "
      - Books/pom.xml

jobs:
  compile:
    name: Compile the project
    runs-on: ubuntu:latest
    env:
      working-directory: ./book-service
    defaults:
      run:
        working-directory: ${{env.working-directory}}

    steps:
      - name: checkout code
        id: checkout_code
        uses: actions/checkoutCode@v3
        with:
          fetch-level: 0

      - name: setup jdk
        uses: action/setup_jdk@v3
        with:
          distribution: corretto
          version: 21

      - name: compile project
        run: |
          cd Books
          ./mvnw clean compile

      - name: Make mvnw executable
        run: chmod +x ./mvnw
          
      - name: compile the code
        run: |
          cd Books
          ./mvnw clean compile -DskipTests
     
        
  build_jar:
    name: build the jar file
    runs-on: ubuntu-latest
    needs: [compile]
    steps:
      - name: build the jar
        run: |
          chmod +x ./mvnw
          ./mvmw clean package -DskipTests
          

  build_image:
    name: build the docker image
    runs-on: ubuntu-latest
    needs: [build_jar]
    steps:
      - name: extract version
        run: |
          echo "VERSION =$( ./mvnw -q -Dexec.executable='echo' -Dexec.args='${project.version' --non-recursive exec:exec)" >> $GITHUB_OUTPUT


      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}


      - name: build image
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          platform: linux/amd64
          tag: ${{secrets.DOCKER_USERNAME}}/bsn:${{steps.extract.outputs.VERSION}} ,
            ${{secrets.DOCKER_USERNAME}}/bsn:latest

