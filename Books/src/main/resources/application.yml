spring:
  application.name: BookService

  datasource:
    password: ${DB_PASSWORD:Oea75357@}
    url: ${DB_URL:jdbc:mysql://localhost:3306/Bookdb?createDatabaseIfNotExist=true}
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USER:root}

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8082
  shutdown: GRACEFUL
